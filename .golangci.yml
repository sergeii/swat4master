version: "2"
linters:
  default: all
  disable:
    - depguard
    - err113
    - exhaustruct
    - funcorder
    - funlen
    - gochecknoglobals
    - inamedparam
    - ireturn
    - maintidx
    - mnd
    - nlreturn
    - noctx
    - noinlineerr
    - paralleltest
    - protogetter
    - tagliatelle
    - varnamelen
    - wrapcheck
    - wsl
    - wsl_v5
  settings:
    cyclop:
      max-complexity: 10
      package-average: 0
    staticcheck:
      checks:
        - all
        - '-QF1008'  # "could remove embedded field "..." from selector"
    modernize:
      disable:
        - fmtappendf
    godot:
      exclude:
        - "^\\s*@"  # swagger/godoc annotations
    nolintlint:
      allow-unused: false
    revive:
      enable-default-rules: true
      rules:
        - name: var-naming
          disabled: true
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - cyclop
        path: (.+)_test\.go
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - gofumpt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
